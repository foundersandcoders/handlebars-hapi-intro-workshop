{{!--Partial for header--}}
<header>
  <div class="container header">
    <h1 class="header-title">Intro to Handlebars</h1>
    <span class="welcome-message">
      Hello {{user.firstName}}
      {{#isAdmin}}
        ({{user.role}})
      {{/isAdmin}}
    </span>
  </div>
</header>
